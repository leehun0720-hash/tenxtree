# 한 그루의 나무가 하나의 데이터가 될 때

**사단법인 리본디어스 × 텐에이아이 — 그린 AX 파트너십 랜딩페이지**

🌐 **[tenxtree.vercel.app](https://tenxtree.vercel.app/)** · 미러: [leehun0720-hash.github.io/tenxtree](https://leehun0720-hash.github.io/tenxtree/)

두 주소 모두 같은 커밋을 서빙합니다. **대표 주소는 Vercel**이며 `canonical`·`og:url`·JSON-LD `url` 이 이쪽을 가리킵니다.

나무를 심는 것은 사람이 하고, 기록하고 증명하는 일은 AI가 합니다.
사단법인 리본디어스의 숲 조성 사업과 ㈜텐에이아이의 AI 기술을 잇는 공동사업 소개 페이지입니다.

> **2026-08-11 브랜드 정정** — 트리헌드레드가 운영 정책 변경으로 온라인 몰이 되면서,
> 파트너 표기를 **사단법인 리본디어스([reborntheearth.org](https://reborntheearth.org))** 로 전면 교체했습니다.
> 단순 치환이 아니라, 리본디어스의 실제 사업분야(숲 조성 및 복원 / 지역경제 활성화 / 탄소중립 페스티벌)를
> 근거로 소개 문구를 다시 썼고, 트리헌드레드 캠페인의 정체성이던 '1인 100그루' 표현은 걷어냈습니다.

**타겟** — 특정 업종이나 기관 유형으로 한정하지 않습니다. **AI 전환(AX)과 ESG가 필요한 조직**이면 기업·공공기관·지자체·학교·재단 어디든 대상이며, 시민 참여 경로도 함께 둡니다.

---

## 구성

```
index.html              전체 페이지 (단일 파일, 빌드 불필요)
assets/earth.mp4        영상 밴드 배경 · 1600×900 · 16초 무한 루프 · 5.4MB
assets/earth-poster.jpg 영상 포스터 (모션 최소화 설정 시 대체 표시)
PLAN.md                 기획안 (v2 현행 / v1 기록)
```

브라우저에서 `index.html`을 열면 바로 동작합니다. 별도 빌드나 서버가 필요 없습니다.
`assets/` 폴더는 `index.html`과 같은 위치에 있어야 영상이 재생됩니다.

## 페이지 구조

| # | 섹션 | 내용 |
|---|---|---|
| 01 | Hero | 한 그루의 나무가 하나의 데이터가 될 때 |
| 02 | 프롤로그 | 심는 일에는 사람이, 남기는 일에는 기록이 |
| 03 | 영상 밴드 | 뜨거워진 지구의 온도를 낮추기 위해 |
| 04 | 두 기관 | 리본디어스 사업 5가지 / 텐에이아이 사업 5가지 |
| 05 | 왜 함께 | 3가지 이유 |
| 06 | 한 그루가 모여 숲이 | 나무 100그루 스크롤 시각화 + 참여 규모 산술 |
| 07 | 시너지 여정 | 나무 한 그루가 정책이 되기까지 5단계 |
| 08 | 함께 만드는 것 | 교육·행사 / 캠페인 플랫폼 / 데이터 대시보드 |
| 09 | 숫자 | 1차년도 목표 |
| 10 | 참여 방법 | AI·ESG가 필요한 조직 / 후원·파트너십 / 시민 |
| 11 | 로드맵 · 문의 · 푸터 | |

## 디자인 시스템

숲 조성 사업의 정체성에 맞춘 딥 포레스트 그린 + 웜 페이퍼 조합입니다.

| 토큰 | 값 | 비고 |
|---|---|---|
| `--forest` | `#09381F` | 딥 포레스트 — 숲 조성 사업의 정체성 |
| `--paper` | `#F8F7F4` | 웜 페이퍼 (reborntheearth.org 의 `#FCFAF8` 계열) |
| `--ink` | `#212121` | 본문 (reborntheearth.org 실측값) |
| `--gold` | `#8C6425` | 라벨 악센트 (페이퍼 위 4.9:1) |
| `--ink-mute` | `#616159` | 보조 텍스트 (4.5:1 이상) |

**폰트** — 본문 Pretendard, 라벨·숫자 Montserrat (리본디어스 사이트에서도 쓰는 서체), 헤드라인 Nanum Myeongjo.

## 모션

GSAP 3.13 + ScrollTrigger (CDN, defer). 총 75개 트리거 중 **스크럽은 6개**이며, 스크럽 대상 속성은
`transform` · `opacity` · `clip-path` 뿐이라 컴포지터에서 처리됩니다. 강제 스크롤 측정 시 중간값 16.7ms(60fps).

| 구간 | 기법 | 참고 |
|---|---|---|
| 히어로 | 단어 마스크 라이즈 + 배경 패럴랙스·스케일 | motion.dev |
| 전 헤드라인 | 어절 단위 스플릿 후 스태거 라이즈 | motion.dev |
| 마퀴 | `modifiers` 기반 이음매 없는 무한 루프 | gsap.com |
| 영상 밴드 | 스크롤에 따라 `clip-path inset` 이 열림 | motion.dev |
| 사진 | 좌→우 클립 와이프 + 내부 이미지 스케일 아웃 | motion.dev |
| 나무 100그루 | 중앙에서 퍼지는 10×10 그리드 스태거 | animejs.com |
| 시너지 여정 | 스크롤 진행선 · 스텝 룰 드로잉 · CSS 3D 틸트 | gsap.com / threejs.org |
| 참여 방법 | SVG 라인 드로잉 (`stroke-dashoffset`) | animejs.com |
| 버튼 | `quickTo` 마그네틱 커서 추적 | motion.dev |
| 네비 | 하강 시 숨김 / 상승 시 노출 | gsap.com |

**three.js는 넣지 않았습니다.** GSAP·Motion 공식 사이트도 홈페이지에 WebGL을 쓰지 않습니다(실측 확인).
사진 중심 편집 지면에 600KB WebGL을 얹으면 무게 대비 얻는 게 없고, 걷어냈던 "테크 데모" 인상이 돌아옵니다.
깊이가 필요한 곳(시너지 여정)은 CSS 3D `rotateY` + `transformPerspective` 로 처리했습니다.

### 실패해도 안전한 구조

1. `<html>` 에 `js` 클래스가 붙을 때만 리빌 대상을 숨깁니다 → **JS가 꺼져 있으면 본문이 그대로 보입니다.**
2. GSAP CDN이 막히면 내장 IntersectionObserver 폴백으로 자동 전환됩니다.
3. 4초 안에 모션 레이어가 붙지 않으면 `js` 클래스를 제거해 전부 드러냅니다.
### 모션 정책

이 사이트는 **OS의 '동작 줄이기' 설정과 무관하게 모든 방문자에게 모션을 재생합니다**
(사이트 소유자 결정, 2026-08-11).

`index.html` `<head>` 안의 스위치 하나로 제어합니다.

```js
var RESPECT_REDUCED_MOTION = false;   // false = 항상 재생 (현재)
                                      // true  = OS 설정 존중, ?motion=on 으로 미리보기
```

`true` 로 바꾸면 `prefers-reduced-motion: reduce` 인 방문자에게는 GSAP 경로를 태우지 않고,
관련 CSS 블록(`@media (prefers-reduced-motion:reduce)`)이 다시 활성화됩니다. 코드는 그대로 남겨 두었습니다.

> **참고** — 전정감·멀미가 있는 방문자에게는 큰 폭의 움직임(패럴랙스, 3D 틸트, 무한 마퀴)이
> 불편할 수 있습니다. 접근성 민원이 들어오면 위 값을 `true` 로 바꾸는 것만으로 즉시 되돌릴 수 있습니다.

## 접근성 · 품질

브라우저 실측 기준입니다.

- 390 / 768 / 1440px 가로 스크롤 없음
- 명암비 자동 감사 (배경 합성 계산) 실패 0건 — WCAG AA
- 터치 타겟 40px 미만 0개
- 콘솔 에러 0건
- `prefers-reduced-motion` 대응 코드 유지 (현재는 스위치로 꺼 둠 — 위 '모션 정책' 참고)
- 시맨틱 마크업, skip link, 키보드 내비게이션, JSON-LD 구조화 데이터

## 설정이 필요한 항목

| 항목 | 위치 | 현재 상태 |
|---|---|---|
| 문의 폼 수신처 | `index.html` 스크립트 상단 `FORM_ENDPOINT` | 비어 있음 → 메일 클라이언트로 대체 전송 |
| 사진 | 전체 `<img>` | Unsplash 임시 이미지. 실제 활동 사진으로 교체 필요 |
| OG 이미지 | `<head>` | `og:image` 미설정 (1200×630 필요) |
| 도메인 | `<link rel="canonical">` · `og:url` · JSON-LD `url` | Vercel 주소. 자체 도메인 확정 시 세 곳 모두 교체 |
| 로고 | 헤더·푸터 SVG | 자체 제작 심볼. 공식 로고 확보 시 교체 |

## 참고

- 페이지의 사업 내용·수치·일정은 협의를 위한 것으로, 협약 체결 전까지 법적 구속력이 없습니다.
- 사용된 사진은 분위기 전달을 위한 임시 이미지(Unsplash)이며 실제 캠페인 활동 기록이 아닙니다.

## 관련 사이트

- [reborntheearth.org](https://reborntheearth.org) — 사단법인 리본디어스
- [tenai.kr](https://tenai.kr) — 주식회사 텐에이아이
